import 'package:flutter/material.dart';

class ViewOrders extends StatelessWidget {
  // props ...
  static String id = 'View Orders';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('View Orders'),
        ),
      ),
    );
  }
}
